# Node.js dependencies
/node_modules
/package-lock.json
/yarn.lock

# Environment variables (never commit these!)
.env
.env.local
.env.development
.env.production
.env.test

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# VSCode settings
.vscode/
*.code-workspace

# MacOS / Linux / Windows system files
.DS_Store
Thumbs.db
*.swp

# Build / dist folders (if you use build tools like Vite, Webpack)
dist/
build/

# Supabase generated files (if any local configuration)
.supabase/

# Optional: ignore local assets that are too large (if needed)
# /assets/*
